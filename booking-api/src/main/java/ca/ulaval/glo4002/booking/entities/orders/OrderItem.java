package ca.ulaval.glo4002.booking.entities.orders;

public abstract class OrderItem implements Priceable {

    // This seems useless for now, but it is almost sure information will be stored on order items, whatever the items
    // actually are.
}
